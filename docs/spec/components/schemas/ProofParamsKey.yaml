type: object
required:
  - id
  - type
properties:
  id:
    type: string
    example: "user@example.com"
    description: "User identifier (e.g., email, address)."
  type:
    type: string
    enum:
      - get_proof_params