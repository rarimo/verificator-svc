type: object
required:
  - id
  - type
properties:
  id:
    type: string
    example: "example@example.com"
    description: "User identifier (e.g., email, address)."
  type:
    type: string
    enum:
      - user-v2