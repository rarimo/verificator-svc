allOf:
  - $ref: '#/components/schemas/StatusKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - status
        properties:
          status:
            type: string
            enum: [verified, not_verified]
